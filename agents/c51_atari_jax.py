"""
DEPRECATED — this was a verbatim copy of CleanRL's c51_atari_jax.py.

It has been superseded by the real JAXAtari port:

    agents/c51_jaxtari.py   (single file, pixel + object-centric modes)

This stub remains only because the file could not be deleted automatically from the
dev environment (the sandbox shell can't reach the WSL filesystem). Safe to delete.
"""

raise SystemExit(
    "agents/c51_atari_jax.py is deprecated. Use agents/c51_jaxtari.py instead."
)
